import React from 'react';
import PropTypes from 'prop-types';

const Clients = props => {
    return (
        <div>
            <h1>Clients</h1>
        </div>
    );
};

Clients.propTypes = {
    
};

export default Clients;